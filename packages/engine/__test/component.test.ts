import { assert, expect, test } from 'vitest';
import { ModelRenderComponent } from '../src/engine/model-render-component';
import * as THREE from 'three';

test('Math.sqrt()', () => {
  const m = new ModelRenderComponent();
  m.ref = new THREE.Group();

  // m.update(0);
});
